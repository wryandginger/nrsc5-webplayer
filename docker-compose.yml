version: '3.8'

services:
  nrsc5-webplayer:
    build: .
    container_name: nrsc5-webplayer
    privileged: true
    ports:
      - "7430:7430"
    devices:
      - /dev/bus/usb:/dev/bus/usb
    environment:
      - TZ=UTC
    network_mode: "host"
    restart: unless-stopped
