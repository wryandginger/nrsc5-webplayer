version: '3.8'

services:
  nrsc5-webplayer:
    build: .
    container_name: nrsc5-webplayer
    ports:
      - "7430:7430"
    devices:
      - /dev/bus/usb:/dev/bus/usb
    environment:
      - TZ=America/Los_Angeles
    network_mode: "host"
    restart: unless-stopped
